{{! TODO:
    [ ]: design the layout for the message here
    [ ]: include  profile pic, username, timestamp & message text
    [ ]: conditionally assign a class if the user is 'sender'
         so that the message will format right-aligned,
         otherwise it will be left-aligned
}}


<form class='message-form'>
<input type="text" class="sender" placeholder="tella">
    <button type='submit' class='send'>
      <i class="bi bi-arrow-up-circle-fill"></i>
    </button>
</form>

<div class="msg-right">
  {{!-- <img src="avatar"> --}}
  {{!-- display=username --}}
  {{!-- timestamp --}}
  {{message}}
</div>


<div class="msg-left">
   {{!-- <img src="avatar"> --}}
  {{!-- display=username --}}
  {{!-- timestamp --}}
{{message}}
</div>


<script src="/public/scripts/message.js"></script>
